spring:
  datasource:
    url: jdbc:h2:mem:sports_lobby_test;MODE=PostgreSQL;DATABASE_TO_UPPER=false;DB_CLOSE_DELAY=-1
    username: sa
    password:
    driver-class-name: org.h2.Driver
  flyway:
    enabled: false
debug: false
logging:
  level:
    org.springframework: INFO
app:
  auth:
    access-token-secret: test-only-change-me-test-only-change-me-32
