# Copy to .env.production only when preparing a release build.
# Values in mobile builds are public identifiers, never server secrets.
APP_ENV=production
API_BASE_URL=https://api.example.com/api/v1
GOOGLE_WEB_CLIENT_ID=
GOOGLE_IOS_CLIENT_ID=

