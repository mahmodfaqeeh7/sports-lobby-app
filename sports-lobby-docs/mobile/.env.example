# Copy to .env for local builds. Values bundled into a mobile application are
# public, so never put server credentials or signing passwords here.
API_BASE_URL=https://api.example.com/api/v1
GOOGLE_WEB_CLIENT_ID=
GOOGLE_IOS_CLIENT_ID=
